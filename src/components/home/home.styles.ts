import type { CSSProperties } from "styled-components";

export const homeStyles: Record<"homeContainer", CSSProperties> = {
  homeContainer: {
    display: "flex",
    flexDirection: "column",
  },
};
